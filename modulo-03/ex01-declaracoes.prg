FUNCTION Main()

    LOCAL cNome := "Nicolly"
    LOCAL nSalario:= 1800.00
    LOCAL lAtivo:= .T.
    LOCAL dAdmissao:= Date()
    LOCAL nCodDepartamento:= 10

    ? "Nome: ", cNome
    ? "Salário: ", nSalario
    ? "Ativo: ", lAtivo
    ? "Data de Admissão: ", dAdmissao
    ? "Código do Departamento: ", nCodDepartamento

RETURN NIL


    